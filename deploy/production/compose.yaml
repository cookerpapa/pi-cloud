name: pi-cloud-production

x-logging: &bounded-logging
  driver: json-file
  options:
    max-file: "3"
    max-size: 10m

x-application-build-args: &application-build-args
  PI_CLOUD_VERSION: ${PI_CLOUD_IMAGE_VERSION:-production}
  PI_CLOUD_REVISION: ${PI_CLOUD_IMAGE_REVISION:-unknown}
  HTTP_PROXY: ${HTTP_PROXY:-}
  HTTPS_PROXY: ${HTTPS_PROXY:-}
  NO_PROXY: ${NO_PROXY:-127.0.0.1,localhost}
  NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org}

x-application-security: &application-security
  read_only: true
  init: true
  cap_drop:
    - ALL
  security_opt:
    - no-new-privileges:true

x-control-plane-runtime-environment: &control-plane-runtime-environment
  NODE_ENV: production
  PI_CLOUD_IMAGE_REVISION: ${PI_CLOUD_IMAGE_REVISION:-unknown}
  HOST: 0.0.0.0
  PORT: "3000"
  DATABASE_URL_FILE: /run/pi-cloud-secrets/database-url
  PI_CLOUD_SUPERVISOR_ENROLLMENT_TOKEN_FILE: /run/pi-cloud-secrets/supervisor-enrollment-token
  PI_CLOUD_SUPERVISOR_MANAGEMENT_TOKEN_FILE: /run/pi-cloud-secrets/supervisor-management-token
  PI_CLOUD_MODEL_CREDENTIAL_MASTER_KEY_FILE: /run/pi-cloud-secrets/model-credential-master-key
  PI_CLOUD_CUBE_EGRESS_CONFIG_TOKEN_FILE: /run/pi-cloud-secrets/cube-egress-config-token
  PI_CLOUD_KAFKA_BROKERS: kafka-1:9092,kafka-2:9092,kafka-3:9092
  PI_CLOUD_KAFKA_PARTITIONS: ${PI_CLOUD_KAFKA_PARTITIONS:-32}
  PI_CLOUD_KAFKA_REPLICAS: "3"
  PI_CLOUD_ACCEPTED_FACT_RETENTION_MS: ${PI_CLOUD_ACCEPTED_FACT_RETENTION_MS:-7200000}
  PI_CLOUD_FACT_CHANNEL_LEASE_MS: ${PI_CLOUD_FACT_CHANNEL_LEASE_MS:-9000}
  PI_CLOUD_FACT_CHANNEL_MAXIMUM_ACTIVE: ${PI_CLOUD_FACT_CHANNEL_MAXIMUM_ACTIVE:-128}
  PI_CLOUD_WORKER_EVENT_INGEST_TOKEN_FILE: /run/pi-cloud-secrets/worker-event-ingest-token
  PI_CLOUD_TOOL_BROKER_URLS: http://tool-broker:4300
  PI_CLOUD_SANDBOX_MATERIALIZER_TOKEN_FILE: /run/pi-cloud-secrets/sandbox-materializer-token
  PI_CLOUD_WORKSPACE_TERMINAL_TOKEN_FILE: /run/pi-cloud-secrets/workspace-terminal-token
  PI_CLOUD_PREVIEW_ORIGIN_BASE_URL: ${PI_CLOUD_PREVIEW_ORIGIN_BASE_URL:-http://preview.localhost:8080}
  PI_CLOUD_SSH_GATEWAY_ENABLED: ${PI_CLOUD_SSH_GATEWAY_ENABLED:-true}
  PI_CLOUD_SSH_ADVERTISED_HOST: ${PI_CLOUD_SSH_ADVERTISED_HOST:-127.0.0.1}
  PI_CLOUD_SSH_ADVERTISED_PORT: ${PI_CLOUD_SSH_PORT:-2222}
  PI_CLOUD_SSH_TICKET_TTL_MS: ${PI_CLOUD_SSH_TICKET_TTL_MS:-86400000}
  PI_CLOUD_ALLOW_INSECURE_INTERNAL_HTTP: "true"
  PI_CLOUD_SUPERVISOR_ID_PREFIX: ${PI_CLOUD_SUPERVISOR_ID_PREFIX:-pi-cloud-worker-}
  PI_CLOUD_SUPERVISOR_MAXIMUM_CAPACITY: ${PI_CLOUD_SUPERVISOR_CAPACITY:-2}
  PI_CLOUD_PLATFORM_MODEL_SOURCE_TENANT_ID: ${PI_CLOUD_PLATFORM_MODEL_SOURCE_TENANT_ID:?run npm run production:init first}
  PI_CLOUD_PLATFORM_OPERATOR_TENANT_ID: ${PI_CLOUD_PLATFORM_OPERATOR_TENANT_ID:-}
  PI_CLOUD_WEB_SESSION_COOKIE_SECURE: ${PI_CLOUD_WEB_SESSION_COOKIE_SECURE:-false}
  PI_CLOUD_WEB_SESSION_TTL_MS: ${PI_CLOUD_WEB_SESSION_TTL_MS:-2592000000}
  PI_CLOUD_SUPERVISOR_MANAGEMENT_URL_TEMPLATES: ${PI_CLOUD_SUPERVISOR_MANAGEMENT_URL_TEMPLATES:-http://{supervisorId}:4100}
  PI_CLOUD_PUBLIC_REGISTRATION_ENABLED: ${PI_CLOUD_PUBLIC_REGISTRATION_ENABLED:-true}
  PI_CLOUD_PUBLIC_REGISTRATION_MAXIMUM_TENANTS: ${PI_CLOUD_PUBLIC_REGISTRATION_MAXIMUM_TENANTS:-1000}
  PI_CLOUD_PUBLIC_TENANT_MAXIMUM_PROJECTS: ${PI_CLOUD_PUBLIC_TENANT_MAXIMUM_PROJECTS:-10}
  PI_CLOUD_PUBLIC_TENANT_MAXIMUM_SESSIONS: ${PI_CLOUD_PUBLIC_TENANT_MAXIMUM_SESSIONS:-100}
  PI_CLOUD_PUBLIC_TENANT_MAXIMUM_UNSETTLED_TURNS: ${PI_CLOUD_PUBLIC_TENANT_MAXIMUM_UNSETTLED_TURNS:-10}
  PI_CLOUD_PUBLIC_TENANT_MAXIMUM_CONCURRENT_TURNS: ${PI_CLOUD_PUBLIC_TENANT_MAXIMUM_CONCURRENT_TURNS:-4}
  PI_CLOUD_PUBLIC_TENANT_MAXIMUM_ACTIVE_SANDBOXES: ${PI_CLOUD_PUBLIC_TENANT_MAXIMUM_ACTIVE_SANDBOXES:-2}
  PI_CLOUD_METRICS_HOST: 0.0.0.0
  PI_CLOUD_METRICS_PORT: "9464"
  PI_CLOUD_METRICS_TOKEN_FILE: /run/pi-cloud-secrets/metrics-token
  PI_CLOUD_OTLP_TRACES_ENDPOINT: ${PI_CLOUD_OTLP_TRACES_ENDPOINT:-}

x-bootstrap-environment: &bootstrap-environment
  NODE_ENV: production
  DATABASE_URL_FILE: /run/pi-cloud-secrets/database-url
  PI_CLOUD_API_TOKEN_FILE: /run/pi-cloud-secrets/api-token
  PI_CLOUD_TENANT_ID: ${PI_CLOUD_TENANT_ID:?run npm run production:init first}
  PI_CLOUD_TENANT_SLUG: ${PI_CLOUD_TENANT_SLUG:-pi-cloud}
  PI_CLOUD_USER_ID: ${PI_CLOUD_USER_ID:?run npm run production:init first}
  PI_CLOUD_API_CREDENTIAL_ID: ${PI_CLOUD_API_CREDENTIAL_ID:?run npm run production:init first}
  PI_CLOUD_CREDENTIAL_BINDING_ID: ${PI_CLOUD_CREDENTIAL_BINDING_ID:?run npm run production:init first}
  PI_CLOUD_DEFAULT_MODEL_PROFILE_ID: ${PI_CLOUD_DEFAULT_MODEL_PROFILE_ID:?run npm run production:init first}
  PI_CLOUD_MODEL_PROFILE_NAME: deterministic-java-repair
  PI_CLOUD_TENANT_MAXIMUM_PROJECTS: ${PI_CLOUD_TENANT_MAXIMUM_PROJECTS:-100}
  PI_CLOUD_TENANT_MAXIMUM_SESSIONS: ${PI_CLOUD_TENANT_MAXIMUM_SESSIONS:-1000}
  PI_CLOUD_TENANT_MAXIMUM_UNSETTLED_TURNS: ${PI_CLOUD_TENANT_MAXIMUM_UNSETTLED_TURNS:-100}
  PI_CLOUD_TENANT_MAXIMUM_CONCURRENT_TURNS: ${PI_CLOUD_TENANT_MAXIMUM_CONCURRENT_TURNS:-4}
  PI_CLOUD_TENANT_MAXIMUM_ACTIVE_SANDBOXES: ${PI_CLOUD_TENANT_MAXIMUM_ACTIVE_SANDBOXES:-64}
  PI_CLOUD_SANDBOX_DOMAINS_JSON: >-
    [{"id":"sandbox-domain-0001","displayName":"Primary sandbox domain","state":"active","toolBrokerBaseUrl":"http://tool-broker:4300","workspaceStorageKey":"workspace-domain-0001","maximumActiveSandboxes":1024}]

x-control-plane-runtime-secrets: &control-plane-runtime-secrets
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/database-url
    target: /run/pi-cloud-secrets/database-url
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/supervisor-enrollment-token
    target: /run/pi-cloud-secrets/supervisor-enrollment-token
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/supervisor-management-token
    target: /run/pi-cloud-secrets/supervisor-management-token
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/model-credential-master-key
    target: /run/pi-cloud-secrets/model-credential-master-key
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/cube-egress-config-token
    target: /run/pi-cloud-secrets/cube-egress-config-token
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/sandbox-materializer-token
    target: /run/pi-cloud-secrets/sandbox-materializer-token
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/workspace-terminal-token
    target: /run/pi-cloud-secrets/workspace-terminal-token
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/worker-event-ingest-token
    target: /run/pi-cloud-secrets/worker-event-ingest-token
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/metrics-token
    target: /run/pi-cloud-secrets/metrics-token
    read_only: true

x-bootstrap-secrets: &bootstrap-secrets
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/database-url
    target: /run/pi-cloud-secrets/database-url
    read_only: true
  - type: bind
    source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/api-token
    target: /run/pi-cloud-secrets/api-token
    read_only: true

services:
  postgres:
    image: postgres@sha256:ef257d85f76e48da1c64832459b59fcaba1a4dac97bf5d7450c77753542eee94
    restart: unless-stopped
    environment:
      POSTGRES_DB: pi_cloud
      POSTGRES_USER: pi_cloud
      POSTGRES_PASSWORD_FILE: /run/pi-cloud-secrets/postgres-password
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/postgres-password
        target: /run/pi-cloud-secrets/postgres-password
        read_only: true
    networks:
      - database
    healthcheck:
      test: [CMD-SHELL, "pg_isready -U pi_cloud -d pi_cloud"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    shm_size: 128m
    pids_limit: 256
    mem_limit: 768m
    cpus: 1.5
    stop_grace_period: 30s
    security_opt:
      - no-new-privileges:true
    logging: *bounded-logging

  kafka-1:
    image: apache/kafka@sha256:3f7b939115cd4872e9cee9369d80bd69712fde55f9902f46d793f64848dedc75
    restart: unless-stopped
    environment: &kafka-environment
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: INTERNAL://:9092,CONTROLLER://:9093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka-1:9093,2@kafka-2:9093,3@kafka-3:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "3"
      KAFKA_OFFSETS_TOPIC_NUM_PARTITIONS: "32"
      KAFKA_OFFSETS_RETENTION_MINUTES: "120"
      KAFKA_HEAP_OPTS: -Xms1g -Xmx1g
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: "3"
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: "2"
      KAFKA_DEFAULT_REPLICATION_FACTOR: "3"
      KAFKA_MIN_INSYNC_REPLICAS: "2"
      KAFKA_NUM_PARTITIONS: "32"
      KAFKA_LOG_RETENTION_MS: ${PI_CLOUD_ACCEPTED_FACT_RETENTION_MS:-7200000}
      KAFKA_LOG_DIRS: /var/lib/kafka/data
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: "0"
      KAFKA_NODE_ID: "1"
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-1:9092
    volumes:
      - kafka-1-data:/var/lib/kafka/data
    networks: [event-log]
    healthcheck:
      test:
        [
          CMD-SHELL,
          "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server 127.0.0.1:9092 >/dev/null 2>&1",
        ]
      interval: 5s
      timeout: 5s
      retries: 45
      start_period: 20s
    mem_limit: 2g
    cpus: 4.0
    logging: *bounded-logging

  kafka-2:
    image: apache/kafka@sha256:3f7b939115cd4872e9cee9369d80bd69712fde55f9902f46d793f64848dedc75
    restart: unless-stopped
    environment:
      <<: *kafka-environment
      KAFKA_NODE_ID: "2"
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-2:9092
    volumes:
      - kafka-2-data:/var/lib/kafka/data
    networks: [event-log]
    healthcheck:
      test:
        [
          CMD-SHELL,
          "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server 127.0.0.1:9092 >/dev/null 2>&1",
        ]
      interval: 5s
      timeout: 5s
      retries: 45
      start_period: 20s
    mem_limit: 2g
    cpus: 4.0
    logging: *bounded-logging

  kafka-3:
    image: apache/kafka@sha256:3f7b939115cd4872e9cee9369d80bd69712fde55f9902f46d793f64848dedc75
    restart: unless-stopped
    environment:
      <<: *kafka-environment
      KAFKA_NODE_ID: "3"
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-3:9092
    volumes:
      - kafka-3-data:/var/lib/kafka/data
    networks: [event-log]
    healthcheck:
      test:
        [
          CMD-SHELL,
          "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server 127.0.0.1:9092 >/dev/null 2>&1",
        ]
      interval: 5s
      timeout: 5s
      retries: 45
      start_period: 20s
    mem_limit: 2g
    cpus: 4.0
    logging: *bounded-logging

  database-bootstrap:
    image: pi-cloud/control-plane:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/control-plane/Dockerfile
      args: *application-build-args
      network: host
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    command: [/app/packages/control-plane/src/bootstrap-main.ts]
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    environment: *bootstrap-environment
    volumes: *bootstrap-secrets
    networks:
      - database
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=32m,mode=1777,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 128
    mem_limit: 384m
    cpus: 1.0
    logging: *bounded-logging

  control-plane:
    image: pi-cloud/control-plane:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/control-plane/Dockerfile
      args: *application-build-args
      network: host
    restart: unless-stopped
    depends_on:
      database-bootstrap:
        condition: service_completed_successfully
      kafka-1:
        condition: service_healthy
      kafka-2:
        condition: service_healthy
      kafka-3:
        condition: service_healthy
    environment: *control-plane-runtime-environment
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    volumes: *control-plane-runtime-secrets
    networks:
      - api
      - database
      - management
      - sandbox-control
      - observability
      - event-log
    healthcheck:
      test:
        [
          CMD,
          node,
          -e,
          "fetch('http://127.0.0.1:3000/health/ready').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m,mode=1777,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 256
    mem_limit: 768m
    cpus: 1.5
    stop_grace_period: 30s
    logging: *bounded-logging

  ssh-gateway:
    image: pi-cloud/ssh-gateway:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/ssh-gateway/Dockerfile
      args: *application-build-args
      network: host
    restart: unless-stopped
    depends_on:
      database-bootstrap:
        condition: service_completed_successfully
      tool-broker:
        condition: service_healthy
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: "2222"
      DATABASE_URL_FILE: /run/pi-cloud-secrets/database-url
      PI_CLOUD_WORKSPACE_TERMINAL_TOKEN_FILE: /run/pi-cloud-secrets/workspace-terminal-token
      PI_CLOUD_SSH_HOST_KEY_FILE: /run/pi-cloud-secrets/ssh-host-key.pem
      PI_CLOUD_ALLOW_INSECURE_INTERNAL_HTTP: "true"
    volumes:
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/database-url
        target: /run/pi-cloud-secrets/database-url
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/workspace-terminal-token
        target: /run/pi-cloud-secrets/workspace-terminal-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/ssh-host-key.pem
        target: /run/pi-cloud-secrets/ssh-host-key.pem
        read_only: true
    ports:
      - ${PI_CLOUD_SSH_BIND_ADDRESS:-127.0.0.1}:${PI_CLOUD_SSH_PORT:-2222}:2222
    networks:
      - edge
      - database
      - sandbox-control
    healthcheck:
      test:
        [
          CMD,
          node,
          -e,
          "const s=require('node:net').connect(2222,'127.0.0.1');s.setTimeout(2000);s.once('connect',()=>{s.destroy();process.exit(0)});s.once('error',()=>process.exit(1));s.once('timeout',()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=16m,mode=1777,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 96
    mem_limit: 192m
    cpus: 0.5
    stop_grace_period: 15s
    logging: *bounded-logging

  provider-relay-volume-bootstrap:
    image: pi-cloud/provider-egress-relay:${PI_CLOUD_IMAGE_VERSION:-production}
    restart: "no"
    entrypoint: [/bin/sh, -eu, -c]
    command:
      - |
        owner=${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}
        chmod 0700 /run/pi-cloud-provider-relay
        chown "$${owner}" /run/pi-cloud-provider-relay
    user: "0:0"
    volumes:
      - provider-relay:/run/pi-cloud-provider-relay
    network_mode: none
    read_only: true
    init: true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    security_opt:
      - no-new-privileges:true
    pids_limit: 16
    mem_limit: 32m
    cpus: 0.1
    logging: *bounded-logging

  provider-host-egress-relay:
    image: pi-cloud/provider-egress-relay:${PI_CLOUD_IMAGE_VERSION:-production}
    restart: unless-stopped
    depends_on:
      provider-relay-volume-bootstrap:
        condition: service_completed_successfully
    environment:
      PI_CLOUD_PROVIDER_RELAY_MODE: host
      PI_CLOUD_PROVIDER_RELAY_SOCKET: /run/pi-cloud-provider-relay/provider-egress.sock
      PI_CLOUD_PROVIDER_RELAY_ALLOWED_HOSTS: api.deepseek.com
      PI_CLOUD_PROVIDER_RELAY_UPSTREAM_PROXY: ${HTTPS_PROXY:-}
    volumes:
      - provider-relay:/run/pi-cloud-provider-relay
    network_mode: host
    healthcheck:
      test:
        [
          CMD,
          node,
          -e,
          "const s=require('node:net').connect({path:'/run/pi-cloud-provider-relay/provider-egress.sock'});s.setTimeout(2000);s.once('connect',()=>{s.destroy();process.exit(0)});s.once('error',()=>process.exit(1));s.once('timeout',()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=8m,mode=1777,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 32
    mem_limit: 64m
    cpus: 0.25
    stop_grace_period: 15s
    logging: *bounded-logging

  provider-egress-relay:
    image: pi-cloud/provider-egress-relay:${PI_CLOUD_IMAGE_VERSION:-production}
    restart: unless-stopped
    depends_on:
      provider-host-egress-relay:
        condition: service_healthy
    environment:
      PI_CLOUD_PROVIDER_RELAY_MODE: bridge
      PI_CLOUD_PROVIDER_RELAY_SOCKET: /run/pi-cloud-provider-relay/provider-egress.sock
      PI_CLOUD_PROVIDER_RELAY_HOST: 0.0.0.0
      PI_CLOUD_PROVIDER_RELAY_PORT: "3129"
    volumes:
      - provider-relay:/run/pi-cloud-provider-relay
    networks:
      - model-egress
    healthcheck:
      test:
        [
          CMD,
          node,
          -e,
          "const s=require('node:net').connect(3129,'127.0.0.1');s.setTimeout(2000);s.once('connect',()=>{s.destroy();process.exit(0)});s.once('error',()=>process.exit(1));s.once('timeout',()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=8m,mode=1777,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 32
    mem_limit: 64m
    cpus: 0.25
    stop_grace_period: 15s
    logging: *bounded-logging

  supervisor-host:
    profiles: [compose-pi-workers]
    image: pi-cloud/supervisor-host:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/supervisor-host/Dockerfile
      args: *application-build-args
      network: host
    restart: unless-stopped
    depends_on:
      control-plane:
        condition: service_healthy
      tool-broker:
        condition: service_healthy
      provider-egress-relay:
        condition: service_healthy
      supervisor-volume-bootstrap:
        condition: service_completed_successfully
    environment:
      NODE_ENV: production
      NODE_USE_ENV_PROXY: "1"
      HTTP_PROXY: http://provider-egress-relay:3129
      HTTPS_PROXY: http://provider-egress-relay:3129
      NO_PROXY: 127.0.0.1,localhost,control-plane,tool-broker,jaeger,postgres,supervisor-host,supervisor-host-1,pi-cloud-worker-1,pi-cloud-worker-2
      DATABASE_URL_FILE: /run/pi-cloud-secrets/database-url
      PI_CLOUD_WORKER_EVENT_INGEST_TOKEN_FILE: /run/pi-cloud-secrets/worker-event-ingest-token
      PI_CLOUD_ALLOW_INSECURE_INTERNAL_HTTP: "true"
      PI_CLOUD_CONTROL_PLANE_URL: http://control-plane:3000
      PI_CLOUD_SUPERVISOR_ID: ${PI_CLOUD_SUPERVISOR_ID_PREFIX:-pi-cloud-worker-}1
      PI_CLOUD_SUPERVISOR_ENROLLMENT_TOKEN_FILE: /run/pi-cloud-secrets/supervisor-enrollment-token
      PI_CLOUD_SUPERVISOR_MANAGEMENT_TOKEN_FILE: /run/pi-cloud-secrets/supervisor-management-token
      PI_CLOUD_TOOL_BROKER_TOKEN_FILE: /run/pi-cloud-secrets/tool-broker-token
      PI_CLOUD_TOOL_BROKER_URLS: http://tool-broker:4300
      PI_CLOUD_TOOL_BROKER_REQUEST_TIMEOUT_MS: "360000"
      PI_CLOUD_MODEL_CREDENTIAL_MASTER_KEY_FILE: /run/pi-cloud-secrets/model-credential-master-key
      PI_CLOUD_SUPERVISOR_MANAGEMENT_HOST: 0.0.0.0
      PI_CLOUD_SUPERVISOR_MANAGEMENT_PORT: "4100"
      PI_CLOUD_SUPERVISOR_MANAGEMENT_ADVERTISED_URL: http://${PI_CLOUD_SUPERVISOR_ID_PREFIX:-pi-cloud-worker-}1:4100
      PI_CLOUD_SUPERVISOR_CAPACITY: ${PI_CLOUD_SUPERVISOR_CAPACITY:-2}
      PI_CLOUD_SUBAGENT_MAXIMUM_DEPTH: ${PI_CLOUD_SUBAGENT_MAXIMUM_DEPTH:-4}
      PI_CLOUD_SUBAGENT_MAXIMUM_NODES: ${PI_CLOUD_SUBAGENT_MAXIMUM_NODES:-32}
      PI_CLOUD_SUBAGENT_MAXIMUM_CONCURRENT: ${PI_CLOUD_SUBAGENT_MAXIMUM_CONCURRENT:-3}
      PI_CLOUD_TRUSTED_WORKSPACE_DIRECTORY: /workspace
      PI_CLOUD_BOOT_STATE_DIRECTORY: /var/lib/pi-cloud/boot-volume/state
      PI_CLOUD_CHECKPOINT_READ_CACHE_TTL_MS: "600000"
      PI_CLOUD_CHECKPOINT_READ_CACHE_MAXIMUM_ENTRIES: "512"
      PI_CLOUD_CHECKPOINT_READ_CACHE_MAXIMUM_BYTES: "33554432"
      PI_CLOUD_MODEL_GATEWAY_HOST: 127.0.0.1
      PI_CLOUD_MODEL_GATEWAY_PORT: "4200"
      PI_CLOUD_MODEL_GATEWAY_ADVERTISED_URL: http://127.0.0.1:4200
      PI_CLOUD_MODEL_GATEWAY_CAPABILITY_TTL_MS: "900000"
      PI_CLOUD_MODEL_GATEWAY_MAXIMUM_REQUESTS_PER_TURN: "128"
      PI_CLOUD_MODEL_GATEWAY_UPSTREAM_REQUEST_TIMEOUT_MS: "120000"
      PI_CLOUD_PI_MODEL_REQUEST_TIMEOUT_MS: "150000"
      PI_CLOUD_PI_TURN_TIMEOUT_MS: "600000"
      PI_CLOUD_METRICS_HOST: 0.0.0.0
      PI_CLOUD_METRICS_PORT: "9465"
      PI_CLOUD_METRICS_TOKEN_FILE: /run/pi-cloud-secrets/metrics-token
      PI_CLOUD_OTLP_TRACES_ENDPOINT: ${PI_CLOUD_OTLP_TRACES_ENDPOINT:-}
    volumes:
      - supervisor-boot:/var/lib/pi-cloud/boot-volume
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/database-url
        target: /run/pi-cloud-secrets/database-url
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/supervisor-enrollment-token
        target: /run/pi-cloud-secrets/supervisor-enrollment-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/supervisor-management-token
        target: /run/pi-cloud-secrets/supervisor-management-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/tool-broker-token
        target: /run/pi-cloud-secrets/tool-broker-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/worker-event-ingest-token
        target: /run/pi-cloud-secrets/worker-event-ingest-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/model-credential-master-key
        target: /run/pi-cloud-secrets/model-credential-master-key
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/metrics-token
        target: /run/pi-cloud-secrets/metrics-token
        read_only: true
    networks:
      management:
        aliases:
          - ${PI_CLOUD_SUPERVISOR_ID_PREFIX:-pi-cloud-worker-}1
      database: {}
      sandbox-control: {}
      model-egress: {}
      observability: {}
      api: {}
    healthcheck:
      test:
        [
          CMD,
          node,
          -e,
          "fetch('http://127.0.0.1:4100/health/ready').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 15s
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m,mode=1777
      - /workspace:rw,noexec,nosuid,nodev,size=16m,mode=0700,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 256
    mem_limit: 2g
    cpus: 2.0
    stop_grace_period: 22m
    logging: *bounded-logging

  supervisor-host-1:
    profiles: [compose-pi-workers]
    extends:
      service: supervisor-host
    environment:
      PI_CLOUD_SUPERVISOR_ID: ${PI_CLOUD_SUPERVISOR_ID_PREFIX:-pi-cloud-worker-}2
      PI_CLOUD_SUPERVISOR_MANAGEMENT_ADVERTISED_URL: http://${PI_CLOUD_SUPERVISOR_ID_PREFIX:-pi-cloud-worker-}2:4100
    volumes:
      - supervisor-1-boot:/var/lib/pi-cloud/boot-volume
    networks:
      management:
        aliases: !override
          - ${PI_CLOUD_SUPERVISOR_ID_PREFIX:-pi-cloud-worker-}2
      database: {}
      sandbox-control: {}
      model-egress: {}
      observability: {}
      api: {}

  supervisor-volume-bootstrap:
    profiles: [compose-pi-workers]
    image: pi-cloud/supervisor-host:${PI_CLOUD_IMAGE_VERSION:-production}
    restart: "no"
    entrypoint: [/bin/sh, -eu, -c]
    command:
      - |
        owner=${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}
        chown -R "$${owner}" /var/lib/pi-cloud/boot-volume /var/lib/pi-cloud/boot-volume-1
        find /var/lib/pi-cloud/boot-volume /var/lib/pi-cloud/boot-volume-1 -type d -exec chmod 0700 {} +
        find /var/lib/pi-cloud/boot-volume /var/lib/pi-cloud/boot-volume-1 -type f -exec chmod 0600 {} +
    user: "0:0"
    volumes:
      - supervisor-boot:/var/lib/pi-cloud/boot-volume
      - supervisor-1-boot:/var/lib/pi-cloud/boot-volume-1
    network_mode: none
    read_only: true
    init: true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
    security_opt:
      - no-new-privileges:true
    pids_limit: 32
    mem_limit: 64m
    cpus: 0.25
    logging: *bounded-logging

  workspace-volume-gateway:
    image: pi-cloud/tool-broker:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/tool-broker/Dockerfile
      args: *application-build-args
      network: host
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    command: [/app/packages/tool-broker/src/workspace-volume-gateway-main.ts]
    environment:
      NODE_ENV: production
      DATABASE_URL_FILE: /run/pi-cloud-secrets/database-url
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_HOST: 0.0.0.0
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_PORT: "4500"
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_TOKEN_FILE: /run/pi-cloud-secrets/workspace-volume-gateway-token
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_MAXIMUM_CONCURRENT_OPERATIONS: ${PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_MAXIMUM_CONCURRENT_OPERATIONS:-2}
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_MAXIMUM_QUEUED_OPERATIONS: ${PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_MAXIMUM_QUEUED_OPERATIONS:-32}
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_QUEUE_WAIT_TIMEOUT_MS: ${PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_QUEUE_WAIT_TIMEOUT_MS:-30000}
      PI_CLOUD_WORKSPACE_POSIX_ROOT: /data/cube-shared/volume
      PI_CLOUD_METRICS_HOST: 0.0.0.0
      PI_CLOUD_METRICS_PORT: "9469"
      PI_CLOUD_METRICS_TOKEN_FILE: /run/pi-cloud-secrets/metrics-token
    volumes:
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/database-url
        target: /run/pi-cloud-secrets/database-url
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/state/cube-shared/volume
        target: /data/cube-shared/volume
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/workspace-volume-gateway-token
        target: /run/pi-cloud-secrets/workspace-volume-gateway-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/metrics-token
        target: /run/pi-cloud-secrets/metrics-token
        read_only: true
    networks:
      - sandbox-control
      - database
      - observability
    healthcheck:
      test:
        [
          CMD,
          node,
          -e,
          "fetch('http://127.0.0.1:4500/health/ready').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m,mode=1777,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 128
    mem_limit: 512m
    cpus: 1.5
    stop_grace_period: 12m
    logging: *bounded-logging

  tool-broker:
    image: pi-cloud/tool-broker:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/tool-broker/Dockerfile
      args: *application-build-args
      network: host
    restart: unless-stopped
    depends_on:
      workspace-volume-gateway:
        condition: service_healthy
      database-bootstrap:
        condition: service_completed_successfully
    environment:
      NODE_ENV: production
      DATABASE_URL_FILE: /run/pi-cloud-secrets/database-url
      PI_CLOUD_SANDBOX_DOMAIN_ID: sandbox-domain-0001
      PI_CLOUD_TOOL_BROKER_ADVERTISED_URL: http://tool-broker:4300
      PI_CLOUD_TOOL_BROKER_HOST: 0.0.0.0
      PI_CLOUD_TOOL_BROKER_PORT: "4300"
      PI_CLOUD_SANDBOX_WARM_TTL_MS: ${PI_CLOUD_SANDBOX_WARM_TTL_MS:-900000}
      PI_CLOUD_MAXIMUM_ACTIVE_TOOL_SANDBOXES: ${PI_CLOUD_MAXIMUM_ACTIVE_TOOL_SANDBOXES:-2}
      PI_CLOUD_MAXIMUM_WARM_SANDBOXES: ${PI_CLOUD_MAXIMUM_WARM_SANDBOXES:-4}
      PI_CLOUD_TOOL_BROKER_OWNERSHIP_LEASE_MS: ${PI_CLOUD_TOOL_BROKER_OWNERSHIP_LEASE_MS:-15000}
      PI_CLOUD_TOOL_BROKER_OWNERSHIP_HEARTBEAT_MS: ${PI_CLOUD_TOOL_BROKER_OWNERSHIP_HEARTBEAT_MS:-5000}
      PI_CLOUD_TOOL_BROKER_TOKEN_FILE: /run/pi-cloud-secrets/tool-broker-token
      PI_CLOUD_SANDBOX_MATERIALIZER_TOKEN_FILE: /run/pi-cloud-secrets/sandbox-materializer-token
      PI_CLOUD_WORKSPACE_TERMINAL_TOKEN_FILE: /run/pi-cloud-secrets/workspace-terminal-token
      PI_CLOUD_CUBE_PERSISTENT_STATE_KEY_FILE: /run/pi-cloud-secrets/cube-persistent-state-key
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_URL: http://workspace-volume-gateway:4500
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_TOKEN_FILE: /run/pi-cloud-secrets/workspace-volume-gateway-token
      PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_REQUEST_TIMEOUT_MS: ${PI_CLOUD_WORKSPACE_VOLUME_GATEWAY_REQUEST_TIMEOUT_MS:-660000}
      PI_CLOUD_WORKSPACE_DELETION_REAPER_INTERVAL_MS: ${PI_CLOUD_WORKSPACE_DELETION_REAPER_INTERVAL_MS:-30000}
      PI_CLOUD_WORKSPACE_DELETION_REAPER_BATCH_SIZE: ${PI_CLOUD_WORKSPACE_DELETION_REAPER_BATCH_SIZE:-16}
      PI_CLOUD_CUBESANDBOX_DIRECT_PRIVATE_CIDRS: ${PI_CLOUD_CUBESANDBOX_DIRECT_PRIVATE_CIDRS:-}
      PI_CLOUD_CUBESANDBOX_REQUEST_TIMEOUT_MS: ${PI_CLOUD_CUBESANDBOX_REQUEST_TIMEOUT_MS:-120000}
      PI_CLOUD_IMAGE_REVISION: ${PI_CLOUD_IMAGE_REVISION:-unknown}
      PI_CLOUD_METRICS_HOST: 0.0.0.0
      PI_CLOUD_METRICS_PORT: "9466"
      PI_CLOUD_METRICS_TOKEN_FILE: /run/pi-cloud-secrets/metrics-token
      PI_CLOUD_OTLP_TRACES_ENDPOINT: ${PI_CLOUD_OTLP_TRACES_ENDPOINT:-}
    volumes:
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/database-url
        target: /run/pi-cloud-secrets/database-url
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/tool-broker-token
        target: /run/pi-cloud-secrets/tool-broker-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/sandbox-materializer-token
        target: /run/pi-cloud-secrets/sandbox-materializer-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/workspace-terminal-token
        target: /run/pi-cloud-secrets/workspace-terminal-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/cube-persistent-state-key
        target: /run/pi-cloud-secrets/cube-persistent-state-key
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/workspace-volume-gateway-token
        target: /run/pi-cloud-secrets/workspace-volume-gateway-token
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/metrics-token
        target: /run/pi-cloud-secrets/metrics-token
        read_only: true
    networks:
      - database
      - sandbox-control
      - observability
    healthcheck:
      test:
        [
          CMD,
          node,
          -e,
          "fetch('http://127.0.0.1:4300/health/ready').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 5s
      timeout: 3s
      retries: 30
      start_period: 10s
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=32m,mode=1777,uid=${PI_CLOUD_APPLICATION_UID:-1000},gid=${PI_CLOUD_APPLICATION_GID:-1000}
    pids_limit: 128
    mem_limit: 384m
    cpus: 1.0
    stop_grace_period: 12m
    logging: *bounded-logging

  web:
    image: pi-cloud/web-ui:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/web-ui/Dockerfile
      args: *application-build-args
      network: host
    restart: unless-stopped
    depends_on:
      control-plane:
        condition: service_healthy
    environment:
      XDG_CONFIG_HOME: /tmp/caddy-config
      XDG_DATA_HOME: /tmp/caddy-data
    ports:
      - ${PI_CLOUD_HTTP_BIND_ADDRESS:-127.0.0.1}:${PI_CLOUD_HTTP_PORT:-8080}:8080
    networks:
      - edge
      - api
    healthcheck:
      test: [CMD, wget, --quiet, --spider, http://127.0.0.1:8080/healthz]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=32m,mode=1777,uid=1000,gid=1000
      - /config:rw,noexec,nosuid,nodev,size=16m,mode=0700,uid=1000,gid=1000
      - /data:rw,noexec,nosuid,nodev,size=16m,mode=0700,uid=1000,gid=1000
    pids_limit: 128
    mem_limit: 128m
    cpus: 0.5
    stop_grace_period: 15s
    logging: *bounded-logging

  observability-volume-bootstrap:
    profiles: [observability]
    image: busybox@sha256:9532d8c39891ca2ecde4d30d7710e01fb739c87a8b9299685c63704296b16028
    restart: "no"
    entrypoint: [/bin/sh, -eu, -c]
    command:
      - |
        owner=${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}
        chown -R "$${owner}" /prometheus /alertmanager /grafana /jaeger
        chmod 0700 /prometheus /alertmanager /grafana /jaeger
    user: "0:0"
    volumes:
      - prometheus-data:/prometheus
      - alertmanager-data:/alertmanager
      - grafana-data:/grafana
      - jaeger-data:/jaeger
    network_mode: none
    read_only: true
    cap_drop: [ALL]
    cap_add: [CHOWN, DAC_OVERRIDE, FOWNER]
    security_opt: [no-new-privileges:true]
    pids_limit: 16
    mem_limit: 32m
    cpus: 0.1
    logging: *bounded-logging

  prometheus:
    profiles: [observability]
    image: prom/prometheus@sha256:63805ebb8d2b3920190daf1cb14a60871b16fd38bed42b857a3182bc621f4996
    restart: unless-stopped
    depends_on:
      observability-volume-bootstrap:
        condition: service_completed_successfully
      control-plane:
        condition: service_healthy
      tool-broker:
        condition: service_healthy
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=15d
      - --web.enable-lifecycle
    volumes:
      - prometheus-data:/prometheus
      - type: bind
        source: ../observability/prometheus.yml
        target: /etc/prometheus/prometheus.yml
        read_only: true
      - type: bind
        source: ../observability/alerts
        target: /etc/prometheus/rules
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/metrics-token
        target: /run/pi-cloud-secrets/metrics-token
        read_only: true
    networks: [observability]
    read_only: true
    init: true
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    pids_limit: 128
    mem_limit: 384m
    cpus: 0.75
    stop_grace_period: 30s
    logging: *bounded-logging

  alertmanager:
    profiles: [observability]
    image: prom/alertmanager@sha256:27c475db5fb156cab31d5c18a4251ac7ed567746a2483ff264516437a39b15ba
    restart: unless-stopped
    depends_on:
      observability-volume-bootstrap:
        condition: service_completed_successfully
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    command:
      - --config.file=/etc/alertmanager/alertmanager.yml
      - --storage.path=/alertmanager
      - --web.external-url=http://127.0.0.1:${PI_CLOUD_ALERTMANAGER_PORT:-9093}
    volumes:
      - alertmanager-data:/alertmanager
      - type: bind
        source: ../observability/alertmanager.yml
        target: /etc/alertmanager/alertmanager.yml
        read_only: true
    networks: [observability]
    read_only: true
    init: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=16m,mode=1777
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    pids_limit: 64
    mem_limit: 128m
    cpus: 0.25
    stop_grace_period: 30s
    logging: *bounded-logging

  jaeger:
    profiles: [observability]
    image: jaegertracing/all-in-one@sha256:836e9b69c88afbedf7683ea7162e179de63b1f981662e83f5ebb68badadc710f
    restart: unless-stopped
    depends_on:
      observability-volume-bootstrap:
        condition: service_completed_successfully
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
      SPAN_STORAGE_TYPE: badger
      BADGER_EPHEMERAL: "false"
      BADGER_DIRECTORY_VALUE: /jaeger/data
      BADGER_DIRECTORY_KEY: /jaeger/key
    volumes:
      - jaeger-data:/jaeger
    networks: [observability]
    read_only: true
    init: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=32m,mode=1777
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    pids_limit: 128
    mem_limit: 384m
    cpus: 0.75
    stop_grace_period: 30s
    logging: *bounded-logging

  grafana:
    profiles: [observability]
    image: grafana/grafana@sha256:a1701c2180249361737a99a01bc770db39381640e4d631825d38ff4535efa47d
    restart: unless-stopped
    depends_on:
      observability-volume-bootstrap:
        condition: service_completed_successfully
      prometheus:
        condition: service_started
      alertmanager:
        condition: service_started
      jaeger:
        condition: service_started
    user: "${PI_CLOUD_APPLICATION_UID:-1000}:${PI_CLOUD_APPLICATION_GID:-1000}"
    environment:
      GF_SECURITY_ADMIN_USER: pi-cloud-admin
      GF_SECURITY_ADMIN_PASSWORD__FILE: /run/pi-cloud-secrets/grafana-admin-password
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "false"
      GF_PATHS_DATA: /grafana
    volumes:
      - grafana-data:/grafana
      - type: bind
        source: ../observability/grafana/provisioning
        target: /etc/grafana/provisioning
        read_only: true
      - type: bind
        source: ../observability/grafana/dashboards
        target: /var/lib/grafana/dashboards
        read_only: true
      - type: bind
        source: ${PI_CLOUD_RUNTIME_DIRECTORY:?run npm run production:init first}/secrets/grafana-admin-password
        target: /run/pi-cloud-secrets/grafana-admin-password
        read_only: true
    networks: [observability]
    read_only: true
    init: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=32m,mode=1777
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    pids_limit: 128
    mem_limit: 256m
    cpus: 0.5
    stop_grace_period: 30s
    logging: *bounded-logging

  observability-ingress:
    profiles: [observability]
    image: pi-cloud/web-ui:${PI_CLOUD_IMAGE_VERSION:-production}
    restart: unless-stopped
    depends_on:
      prometheus:
        condition: service_started
      alertmanager:
        condition: service_started
      jaeger:
        condition: service_started
      grafana:
        condition: service_started
    environment:
      XDG_CONFIG_HOME: /config
      XDG_DATA_HOME: /data
    command: [caddy, run, --config, /etc/caddy/Caddyfile]
    volumes:
      - type: bind
        source: ../observability/Caddyfile
        target: /etc/caddy/Caddyfile
        read_only: true
    ports:
      - 127.0.0.1:${PI_CLOUD_PROMETHEUS_PORT:-9090}:9090
      - 127.0.0.1:${PI_CLOUD_ALERTMANAGER_PORT:-9093}:9093
      - 127.0.0.1:${PI_CLOUD_JAEGER_PORT:-16686}:16686
      - 127.0.0.1:${PI_CLOUD_GRAFANA_PORT:-3001}:3001
    networks:
      - observability
      - observability-edge
    healthcheck:
      test: [CMD, wget, --quiet, --spider, http://127.0.0.1:9090/-/healthy]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    <<: *application-security
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=16m,mode=1777,uid=1000,gid=1000
      - /config:rw,noexec,nosuid,nodev,size=8m,mode=0700,uid=1000,gid=1000
      - /data:rw,noexec,nosuid,nodev,size=8m,mode=0700,uid=1000,gid=1000
    pids_limit: 64
    mem_limit: 64m
    cpus: 0.25
    stop_grace_period: 15s
    logging: *bounded-logging

  provider-egress-relay-image:
    profiles: [image-only]
    image: pi-cloud/provider-egress-relay:${PI_CLOUD_IMAGE_VERSION:-production}
    build:
      context: ../..
      dockerfile: packages/provider-egress-relay/Dockerfile
      args: *application-build-args
      network: none
    network_mode: none
    restart: "no"

networks:
  edge: {}
  api:
    internal: true
  database:
    internal: true
  management:
    internal: true
  sandbox-control:
    internal: true
  model-egress:
    internal: true
  provider-egress: {}
  observability:
    internal: true
  observability-edge: {}
  event-log:
    internal: true

volumes:
  postgres-data: {}
  kafka-1-data: {}
  kafka-2-data: {}
  kafka-3-data: {}
  supervisor-boot: {}
  supervisor-1-boot: {}
  prometheus-data: {}
  alertmanager-data: {}
  grafana-data: {}
  jaeger-data: {}
  provider-relay: {}
