{
  "format": "pi-cloud.fault-eval.v1",
  "description": "Deterministic infrastructure fault contracts for the maintained PostgreSQL, Kafka, Pi Worker and Cube Tool path.",
  "cases": [
    {
      "id": "duplicate-command",
      "workspace": "@pi-cloud/sandbox-supervisor",
      "file": "test/agent-run-supervisor.test.ts",
      "test": "deduplicates the same command and reuses one execution promise",
      "invariant": "Duplicate delivery does not execute one Agent command twice."
    },
    {
      "id": "stale-execution-lease",
      "workspace": "@pi-cloud/sandbox-supervisor",
      "file": "test/agent-run-supervisor.test.ts",
      "test": "retains the high-water fencing token after a pre-start release",
      "invariant": "A released Worker cannot reuse an obsolete ExecutionLease."
    },
    {
      "id": "stale-accepted-fact-authority",
      "workspace": "@pi-cloud/runtime-core",
      "file": "test/session-lease-authority-gate.test.ts",
      "test": "admits one FactChannel, accepts both Fact kinds, renews and closes idempotently",
      "invariant": "Only the current PostgreSQL ExecutionLease can admit CandidateFacts into the durable log."
    },
    {
      "id": "session-mutation-redelivery",
      "workspace": "@pi-cloud/pi-session-postgres",
      "file": "test/postgres-pi-session-storage.test.ts",
      "test": "replays one projected mutation ID as one PostgreSQL effect",
      "setupTests": [
        "persists bounded branch context and durable operation records without a JSONL download"
      ],
      "invariant": "Kafka redelivery creates one canonical Pi Session mutation."
    },
    {
      "id": "session-projection-barrier-before-read",
      "workspace": "@pi-cloud/pi-session-postgres",
      "file": "test/postgres-durable-agent-session.test.ts",
      "test": "waits for older Session mutations before rechecking the current ExecutionLease",
      "invariant": "A replacement Worker observes every older accepted Session mutation before reading context."
    },
    {
      "id": "message-stream-independence",
      "workspace": "@pi-cloud/pi-session-postgres",
      "file": "test/cloud-agent-runtime.test.ts",
      "test": "commits a complete assistant message independently of public event delivery",
      "invariant": "A live-stream failure cannot roll back a complete native Pi message."
    },
    {
      "id": "interrupted-visible-prefix",
      "workspace": "@pi-cloud/pi-session-postgres",
      "file": "test/cloud-agent-runtime.test.ts",
      "test": "keeps a failed visible assistant prefix in the next native Pi context",
      "invariant": "A browser-visible interrupted prefix remains model-visible after Worker replacement."
    },
    {
      "id": "terminal-tail-unload",
      "workspace": "@pi-cloud/runtime-core",
      "file": "test/kafka-live-session-tail.test.ts",
      "test": "deduplicates, snapshots immutably and unloads only after terminal canonical state",
      "invariant": "Terminal projection unloads shared live-tail memory without invalidating an in-flight browser snapshot."
    },
    {
      "id": "multiplexed-fact-reconnect",
      "workspace": "@pi-cloud/runtime-core",
      "file": "test/accepted-fact-channel.test.ts",
      "test": "reopens every logical Run stream after the shared Worker socket reconnects",
      "invariant": "One broken Worker transport reconnects logical Fact Streams without changing their authorities."
    },
    {
      "id": "control-plane-process-sigkill",
      "workspace": "@pi-cloud/sandbox-supervisor",
      "file": "test/control-channel-process-fault.test.ts",
      "test": "keeps an active assignment alive while the Control Plane process is killed and replaced",
      "invariant": "Control Plane process replacement does not revoke a healthy Agent Loop."
    },
    {
      "id": "stale-worker-socket",
      "workspace": "@pi-cloud/control-plane",
      "file": "test/supervisor-websocket-gateway.test.ts",
      "test": "uses PostgreSQL to reject an old socket after reconnecting through another replica",
      "invariant": "A stale Worker Control Channel cannot reclaim current ownership."
    },
    {
      "id": "abandoned-development-environment",
      "workspace": "@pi-cloud/control-plane",
      "file": "test/development-environment-service.test.ts",
      "test": "retires an abandoned pre-provision request without creating a Cube",
      "invariant": "A Control Plane failure before provisioning cannot leak a requested development machine."
    },
    {
      "id": "tool-broker-owner-fence",
      "workspace": "@pi-cloud/tool-broker",
      "file": "test/workspace-runtime-state-repository.test.ts",
      "test": "fences an expired replica before a surviving owner stays Ready",
      "invariant": "A replacement Tool Broker fences the expired owner before serving its Workspace runtime."
    },
    {
      "id": "ambiguous-tool-transport",
      "workspace": "@pi-cloud/tool-broker",
      "file": "test/cubesandbox-provider.test.ts",
      "test": "destroys an uncertain VM without replaying a disconnected Tool command",
      "invariant": "An ambiguous Tool transport never blindly replays an arbitrary command."
    },
    {
      "id": "stale-workspace-settlement",
      "workspace": "@pi-cloud/tool-broker",
      "file": "test/cubesandbox-provider.test.ts",
      "test": "rejects a persistent Volume reference when tenant or fence is stale",
      "invariant": "A stale or foreign settlement cannot become the Workspace head."
    },
    {
      "id": "cancel-stop-failure",
      "workspace": "@pi-cloud/tool-broker",
      "file": "test/tool-broker.test.ts",
      "test": "revokes the Session lease before a provider stop failure escapes",
      "invariant": "Cancellation revokes Tool authority before uncertain process cleanup returns."
    },
    {
      "id": "shared-runtime-loss",
      "workspace": "@pi-cloud/tool-broker",
      "file": "test/tool-broker.test.ts",
      "test": "fails every binding when the shared physical Workspace runtime is lost",
      "invariant": "Loss of one Workspace Cube cannot leave a surviving logical Tool binding."
    },
    {
      "id": "workspace-volume-delete-race",
      "workspace": "@pi-cloud/tool-broker",
      "file": "test/workspace-volume-deletion-reaper.integration.test.ts",
      "test": "waits for the live Cube activation before purging POSIX Workspace data",
      "invariant": "Workspace bytes are not deleted while a live Cube still owns the Volume."
    }
  ]
}
